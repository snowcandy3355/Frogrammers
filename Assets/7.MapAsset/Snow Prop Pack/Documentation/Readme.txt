The asset supports two types of rendering: Built-In Render Pipeline and Universal Render Pipeline(URP). The default is Built-In.

How to install URP version:
-Open or create a new project with Universal Render Pipeline support
-Click Assets> Export Package ...
-Select our Snow Prop Pack URP.unitypackage asset
-Waiting for export and using it.